# Session Handoff

## 1. Current State
**The live score reveal is built, committed and pushed** (`b498c56` on `origin/main`). Checked: `tsc --noEmit`, `eslint src` and 425 tests pass. The user reviewed Enter Results, Finish Week and the Spoiler-Free setting in their own browser and signed off on each round; Home/Standings/Results reading a revealing week (P1a/P1b) have **not** been looked at in a browser. `npm run build` was **not run** (a dev server holds port 3000). **Verified live against the project** (empty far-future Week 11, then cleaned up to zero rows): reveal, undo, autosave can't overwrite a posted couple, the preserve-posted-rows delete filter, and Spoiler-Free on/off/marked visibility of a revealing week via a throwaway account. **Never exercised:** a real Finish Week with couples already posted (running it on Week 11 would complete that week for real). `supabase/apply-unmark-watched.sql` has been applied by the user and tested.

Built this session:
- **Home:** compact Your Leagues rows (max 4, picks-due first, See All), shared "Picks close in …" line over two-column `DeadlineStub` tickets, scrolling Recent Activity (one line per couple, dance score lines, styled names), quieter Create/Join, "Results in" held 48h after publish (final week 7 days), "Pacific feed at 8pm" copy. Fixed: cleared picks (an all-null `predictions` row) now count as picks due (`hasCurtainCallPicks`).
- **Enter Results (`/admin/results`):** per-couple **Publish** with **Undo · Ns** then **Un-Post** (`revealCoupleScores`/`undoCoupleReveal`, `src/lib/results-reveal.ts`, pure state in `src/lib/reveal-state.ts`); views Unpublished / Published / Leaderboard; a **Finish Week** checklist table (status, In Jeopardy, Judges' Save, Immunity, Bonus) with a no-elimination confirm dialog; Judges' Save Active moved to the Schedule episode form (`episodes.judges_save_available`); Score a Team Dance sheet removed.
- **Viewers:** `loadRevealingWeek`/`scoredWeekIds` (`src/lib/revealing-week*.ts`) let Home activity, league points, Standings (rank arrows compare against the revealing week), Score History (judges lines only), Couples Leaderboard, roster cards and Results (`scores` mode) show posted scores; `RevealAutoRefresh` re-fetches every 20s. Spoiler-Free: mid-reveal catch-up callout (no auto dialog), and Settings "I last watched" picker (`unmark_episodes_watched_from`).

## 2. Changes Made
Ours (all committed and pushed): `CLAUDE.md`, `BACKLOG.md`, `MEMORY_HANDOFF.md`, `supabase/{schema.sql,apply-unmark-watched.sql}`, `src/app/{today,this-week,leagues/[id],admin/results}/…` pages and `this-week/actions.ts`, `src/components/{home-dashboard,deadline-stub,create-join-league-dialogs,episode-banner,spoiler-reveal-callout,spoiler-mode-toggle,watched-through-setting,reveal-auto-refresh,results-form,results-screen,schedule-manager,weekly-results-view}.tsx`, `src/lib/{home-activity,curtain-call-picks,reveal-state,results-reveal,revealing-week,revealing-week-data,results,results-draft,results-page-data,results-status,league-home-summary,league-summary,score-history-data,this-week-carousel,episode-banner}.ts` (+ tests), `src/lib/supabase/types.ts` (hand-added `unmark_episodes_watched_from`; regenerate when a token is available).
**Not ours, leave unstaged:** `ios/App/App.xcodeproj/project.pbxproj` (modified); untracked `scratch/` (`prefill-week3-draft.mts`, `league-settings-relocation-plan.md`, `draft-recast-handoff.html` are kept on purpose).

## 3. Key Decisions
- **Revealed scores are final, not provisional.** Reveal = copy the saved draft's dances into live `dance_scores` (no new table; "revealed" = live rows on an unpublished episode) and run `recomputeWeekForReveal` (judges points only; Curtain Call/Grand Finale wait for Finish Week). Undo deletes and recomputes. Finish Week is refused until every couple with a drafted dance is posted and skips rewriting posted dances.
- **Two viewer paths only:** Spoiler-Free off sees reveals as posted; Spoiler-Free on stays hidden until they mark the week watched (allowed mid-reveal; the "I last watched" picker can only lower it). Explicitly cut and not to be rebuilt: watching-live session, Follow live, Live strip, LIVE chips, per-dance marks, Reveal on Home, standalone Reveal console, a "Provisional" pill.
- **Don't add tweaks the user didn't ask for; defer to their explicit instructions over mocks.** The Home curtain **look** change is a separate follow-up (mock first, together with the persistent Spoiler-Free banner).
- **Inner components remount on every render** — the couple entry card is a plain render function for that reason (it fixed inputs taking one character at a time); don't turn it back into a nested component.
- **Older rules still binding:** manager-earned points in Fraunces; only theme colors; Libre Caslon for placement numbers only; no light/dark toggle; Title Case for names, sentence case for sentences.
- Never `npm run build` / `rm -rf .next` while a dev server holds port 3000. Shared working dir: `git fetch` + `git status -sb` first; stage by name, never `git add -A`. Another session's uncommitted edits may appear in files you touch.

## 4. Backlog & Next Steps
All deferred work is in [BACKLOG.md](BACKLOG.md), in priority order at the top: (1) check the reveal on a real episode night (and run `npm run build` once no dev server is running), (2) show the viewer's own points in Dance Card League at a Glance, (3) split Scores and Enter Results into separate pages. Also queued there: the persistent Spoiler-Free banner (needs a mock), the "To check" Schedule settings (Judges' Save, Team Dance round type), and the smaller nits.

`git fetch && git status -sb`
